{% extends 'base.html.twig' %}

{% block titrePage %}Formation{% endblock %}

{% block contenuPage %}
    <h1>Formation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ formation.id }}</td>
            </tr>
            <tr>
                <th>Intitule</th>
                <td>{{ formation.intitule }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ formation.adresse }}</td>
            </tr>
            <tr>
                <th>Telephone</th>
                <td>{{ formation.telephone }}</td>
            </tr>
            <tr>
                <th>Mail</th>
                <td>{{ formation.mail }}</td>
            </tr>
        </tbody>
    </table>


      <a href="{{ path('formation_index') }}" class="btn btn-primary">Retour à la liste</a>

      <a href="{{ path('formation_edit', {'id': formation.id}) }} "class="btn btn-primary">Editer</a> <br><br>

    {{ include('formation/_delete_form.html.twig') }}
{% endblock %}
