{%extends "base.html.twig"%}

  {% block contenu %}
    {% block intro %}
  <br> <br>
  <p>Vous trouverez sur cette application des stages pouvant vous convenir, vous trouverez également les formations et les entreprises concernées. <br>
    Son seul but : vous aidez dans la recherche de stage et trouver le stage vous convenant le plus </p>
    {% endblock %}


    <div class="card-deck">
      {% for stageCourant in stages %}

        <div class="card">
          <a href="{{path('pro_stage_stages',{id:stageCourant.id})}}" class="card-link">
            <img class="card-img-top" src="{{stageCourant.URL}}" alt="Card image cap">
          </a>
        <div class="card-body">
          <h5 class="card-title">{{stageCourant.initule}}</h5>
          <p class="card-text">{{stageCourant.descriptif}}</p>
          <p class="card-text"><b>Entreprise : </b> {{stageCourant.entreprise.intitule}}</p>
        </div>
      </div>

    {% endfor %}

</div>

<nav aria-label="Page navigation example">
  <ul class="pagination justify-content-center">
    <li class="page-item"><a class="page-link" href="#">Previous</a></li>
    <li class="page-item"><a class="page-link" href="#">1</a></li>
    <li class="page-item"><a class="page-link" href="#">2</a></li>
    <li class="page-item"><a class="page-link" href="#">3</a></li>
    <li class="page-item"><a class="page-link" href="#">Next</a></li>
  </ul>
</nav>
  {% endblock %}
